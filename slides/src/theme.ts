// Palette + typo de la chaîne — à réutiliser dans tous les composants
export const COLORS = {
  bg: '#0E1116', // fond bleu nuit
  text: '#F5F5F0', // blanc cassé
  orange: '#FF7A1A', // chaleur / énergie
  blue: '#3FA7FF', // eau / froid
};

// Pour le vrai Poppins : `npm i @remotion/google-fonts` puis loadFont() (voir README).
// Sinon fallback système, le projet tourne quand même.
export const FONT_FAMILY = '"Poppins", "Inter", system-ui, sans-serif';
