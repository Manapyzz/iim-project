{
  "name": "demo-03-data-lookup",
  "version": "1.0.0",
  "private": true,
  "description": "Pattern Lookup Anti-Hallu — donnees dans JSON, LLM les LIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "@types/node": "^22.9.0"
  }
}
